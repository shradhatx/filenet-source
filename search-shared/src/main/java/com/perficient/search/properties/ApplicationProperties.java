package com.perficient.search.properties;

import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties(prefix = "search-services")
public class ApplicationProperties {
   // TODO: add properties we need to load for the services per environment
}
